# Demo source repo

This tiny repository is cloned into the sandbox session to demonstrate the Tethricor
**code-IN** path. The demo harness task appends a line to this file and creates a new
`GENERATED.txt`; both changes are returned as `changes.zip` via the **code-OUT** path.
