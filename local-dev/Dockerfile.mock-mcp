# Local minimal MCP stand-in.
FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY mock_mcp.py .

EXPOSE 9090
CMD ["python", "mock_mcp.py"]
