# Local OpenAI-compatible AgentGateway stand-in.
FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY mock_gateway.py .

EXPOSE 8081
CMD ["python", "mock_gateway.py"]
